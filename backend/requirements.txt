# 锁定到内测验证通过的版本，保证 VPS 全新部署可复现（审计 #5）。
# 故意升级时：改这里的版本号 → 重装 → 跑 run_tests.py 绿了再提交。
fastapi==0.136.3
uvicorn[standard]==0.49.0
sqlalchemy==2.0.50
pydantic==2.13.4
pyyaml==6.0.3
openai==2.41.1
python-dotenv==1.2.2
