# 2026-07-14 · 知返固定讲解策略

## 完成内容

- 新增每轮结构化 `TeachingStrategy`，把讲解目标、解释路径、切路信号、知识断层和代码调用链顺序变成可观测数据。
- 正常轮次先搭整体骨架；学生明确说听不懂或连续无进展时，按“比喻→极小例子→执行链→对照”切换路径。
- 识别下标/索引、函数调用、循环边界、空值等常见前置断层，要求先补最小知识块。
- 代码讲解固定为“入口与输入→谁调用谁→关键数据怎样变化→返回值或报错落点”。
- 策略层只影响讲解组织，不改六阶段状态机、泄题门控、数据库 schema 或事件表。
- `run_turn` 响应新增 `teaching_strategy` 调试字段。

## 验证

- 新增 4 项纯函数/Prompt 测试。
- 全量结果：104 过 / 0 败 / 6 跳过 / 共 110；跳过均因 Docker 不可用。
