# ACP 主线二轮进展：续学决策与回放归拢（2026-07-15）

## 已完成

### 单一下一步

- “我的成长”并行读取未完成会话、到期复习和个性化推荐。
- 固定按“未完成 > 复习 > 推荐 > 新练习”选择一个行动，页面不再同时铺开多道复习题。
- 从成长页续做时可直接恢复指定会话，不必回到训练大厅再点一次。
- 其他待办只显示数量提示，不与当前行动争夺焦点。

### 回放主线

- 后端为完整回放步骤增加纯派生的 `mainline` 与 `mainline_reasons`。
- 主线会纳入首次/最近真实运行、已有转折点、学生观察、学生总结和导师换讲法。
- 回放返回 `mainline_steps`，学生端与教师端消费同一套重点判断。
- 前端默认展示“主线回放”，完整原始过程可一键展开；所有代码、对话和技术凭据仍保留。
- 观察记录和思考总结在回放中去掉内部标记，按自然文本呈现。

## 涉及文件

- `backend/app/services/process.py`
- `backend/run_tests.py`
- `frontend/src/views/Arena.vue`
- `frontend/src/views/Timeline.vue`
- `frontend/src/views/Replay.vue`
- `frontend/e2e/critical-flows.spec.js`
- `docs/计划/2026-07-15-ACP主线二轮-续学决策与回放归拢.md`

## 验证

- Python 与 E2E 脚本语法检查通过。
- Vite production build 通过：52 个模块成功转换。
- 后端全量回归：`118 过 / 0 败 / 6 跳过 / 共 124`；跳过项均为本机无 Docker 的隔离测试。
- Playwright 关键链路：`2 passed (3.5s)`，主线/完整过程切换与唯一下一步断言均通过。

## 当前状态

- 代码、文档和关键链路验证均已完成。
- 第二轮与第三轮共享前端页面和回归用例，按同一个结构性交付单元一起提交。
