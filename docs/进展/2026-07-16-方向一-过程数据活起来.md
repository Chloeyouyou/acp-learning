# 2026-07-16 方向一 · 让过程数据活起来（1.1 + 1.3 完成）

分支 `m-data-alive`（基于 m-prior-layer）。计划见同日期计划文档。

## 盘点发现

规划文档（07-07）里的 **1.2 回放转折点标注在 M3 期间已经做完**
（`process.derive_turning_points`：首次真实反馈/对准关键行/错误形态变化/关键突破
四类标记 + 主线归拢，测试在位）。本轮只做剩下两块。

## 1.1 过程旁白：导师看得见跨局历史模式

- `process.recent_struggle_signal(db, student_id, pattern_id, exclude_session_id)` 纯派生两信号：
  - **同题重开**：之前 ≥2 局没走完 → 「第 N 次打开这道题，都停在某阶段附近」；
  - **试探开局惯性**：近 3 局可判快照样本里 ≥2 局失败后两版以上才碰关键行
    （`process.blind_start` 公共判定，1.3 复用同一逻辑）。
- tutor.py 注入 `STRUGGLE_LAYER_TEMPLATE`，硬规则写死：不照念（不说「你已经第几次」）、
  不动任何门控/提示级别、本轮表现优先。无信号零注入，coop 排除。
- 测试：信号派生正反例 + stub-LLM 捕获 system 验证注入与硬规则齐全。

## 1.3 「我的调试足迹」周报

- `timeline.debug_footprint(db, student_id, episodes, days=7)` 纯派生聚合：
  练了几题/尝试次数/活跃天数/内化清单、最常卡的一步（扶一把轮按 stage_before 聚合、
  翻成四步语言）、从试探走到定向的题、高频思维默认值本周是否松动。
- 挂进 `/timeline` 响应（复用已算好的 episodes，不重复重建）；
  前端「我的成长」页加一条安静的「近 7 天足迹」卡：统计一行 + 进步/松动先讲、
  卡点带一句可行建议；无活动整卡不出现。

## 验证

- 后端主套件 `142 过 / 0 败 / 6 跳过 / 共 148`。
- vite build 过；Playwright 关键链路 2 过（`E2E_PYTHON` 指 backend/.venv）。

## 待办 / 后续

- [ ] 真局手感：旁白会不会被导师过度引用（与先验层注入同一轮观察）。
- [ ] 方向一 1.1 未来可与先验层打通：确定性过程信号（本轮）+ 语义倾向（priors 包）
      各管一段，不重复建设。
- [ ] 周报「松动」目前只看已内化；将来可加「同簇题提示深度下降」这类更细的松动证据。
