# 进展记录 · 2026-06-24

接 `2026-06-23-coop灵犀画像测试.md`。本篇记后续的打磨/扩充/清理。全部已 commit + push 到 GitHub `Chloeyouyou/acp-learning`。

## 已做（已 commit + push）

### 1. 报错翻译收尾
- 闯关「运行」+「提交修复失败」+ coop「运行」三处，报错/超时旁都配中文人话（commit `692139a`、`5096457`）。
- 翻译从「是什么」推进到「+ 下一步怎么查」（拼 ERROR_PLAYBOOK 的 probe）（commit `41330e0`）。

### 2. coop 打磨
- 「解决了」后给「换一道 / 回大厅」出口，闭环补完整（commit `41330e0`）。
- coop 页加「点行号看讲解」（从闯关页搬，新增 tutor.walkthrough_for_code + /api/coop/walkthrough）（commit `59afac8`）。

### 3. 代码质量清理（4 个审查 agent 共识，commit `c25a5bc`）
- A 讲解去重 + 修缓存（walkthrough_for_code 原是 explain_code 整段复制、缓存无界增长且失败也缓存→抽 _walkthrough 核心 + LRU + 失败不缓存）。
- B coop.get() 压平 custom/sample 假字典分支。
- C 防污染收口（TutorSession.is_coop property，timeline/大厅两处都调它）。
- D 抽 tutor.run_and_inject（闯关 run 端点与 coop.run 的跑沙箱→记事实→注入→带翻译 3 处重复合一）。
- E Arena consecutiveStruggles 多余 ref→let。净删码（删 81 加 70）。

### 4. 闯关题库扩充 17 → 23 道
- arithmetic 类 0→4：BP-ARITH-001 整除丢小数(WA)、002 取模写反(WA)、003 累加初值错(WA)（commit `2a259cd`、`e3a0215`）。
- BP-NULL-006 字典漏默认值(RE)、BP-SHARED-001 列表别名(WA)、BP-LOOP-006 递归忘终止(RE)（commit `2a259cd`、`1d41fc9`）。
- 每道均过质检双关（buggy 真触发设计症状 / expected_fix 真判过 / fix_check 匹配），配齐 6 级提示+内化+认知根因。
- 覆盖现状：category 4 类齐(boundary6/null7/loop6/arith4)、6 思维簇都有题、无 0 道窟窿；薄弱处 crash_site(2)/shared_state(3↑)、HANG(3) 仍偏少。
- 测试稳定 84/0。

## 未做（挂起 / 按时机）

### 需用户在 VPS 操作（积压越来越多，新功能 VPS 上看不到）
- **部署上线**：GitHub 到 `1d41fc9`，VPS 仍 6/19 旧版。`git pull && docker build && docker rm -f acp && docker run...(带 -v 挂载)`。网络反复连不上 VPS，多次推迟。
- **查「记录没了」**：`docker exec acp sqlite3 /data/acp.db "select distinct student_id from tutor_sessions"` 找回真实学号。

### 需用户本地验证 UI 效果（我在本机看不到网页）
- 灵犀挫败气泡手感（门槛=连续3次真挣扎、每天1次——数字待真试调）。
- coop 行号讲解的对齐/滚动同步。
- #14 ⑤验证不能跑代码（学习闭环硬伤，待重做 Arena 时改）。

### 等真实数据 / 拍板（诚实画像红线）
- 算分 v2（#18 维度口径/#19 冷启动/#20 Internalization 不进维度）——已先做「覆盖度」缓解 #18。
- coop B2 开分（现只埋点不计分）。
- thinking_pattern 自动推断、提问训练 P2-B——都暂缓（无数据易不诚实/污染画像）。

### 记为技术债（agent 建议先别抽）
- Coop.vue↔Arena 行号讲解抽 Vue 组件、灵犀抽 composable、explain_error HANG 进 ERROR_PLAYBOOK。

### 低风险随手
- #9 CORS、#11/#12/#13、测试隔离 acp.db、#7/#8（随 coop 已部分解决，is_coop 收口了防污染）。
