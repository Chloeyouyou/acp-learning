# 进展记录 · 2026-07-05

大改 v2 启动日。先做了一次架构级全面体检，据此定了两周计划，然后一口气推完 **M1 还债周 + M2 过程化 + M3a 回放**。全部已 commit + push 到 GitHub `Chloeyouyou/acp-learning`（main）。

- 体检报告：`docs/审计/2026-07-05-全面体检-架构与过程化评审.md`
- 升级计划（含每步验收/勾选）：`docs/计划/2026-07-05-v2过程化升级计划.md`

测试基线：`run_tests.py` **84 → 101 过 / 0 败**。

---

## 里程碑 M1 · 还债周（安全/身份/迁移/并发）— 已合并 main（merge `f455b02`）

### D1 沙箱容器化（commit `1ab3c2d`）
- `sandbox.py` 双后端：docker（一次性隔离容器，`--network none`/`--read-only`/`--memory 128m`/`--pids-limit 64`/`--cap-drop ALL`/非 root，代码走 stdin）+ subprocess（本地 dev fallback）。`ACP_SANDBOX=docker/subprocess/auto` 三档。接口 `run_code/RunResult/normalize_output` 不变。
- 逃逸验证 6 条全过：读库/偷 key/出网/死循环/fork 炸弹/写根，全被挡且正常代码照跑。
- Dockerfile 装 docker.io CLI；`/api/health` 暴露 sandbox 后端；部署文档加 socket 挂载 + 预拉镜像。
- **拍板**：docker 挂载 = 宿主 socket 直挂（sibling-container）。

### D2-3 身份 + 越权门（commit `d2f887e`）
- `app/security.py` HMAC 无密码签名 token（零新依赖，secret 取环境变量或 `.auth_secret` 持久化 + gitignore）；`students` 表；`/api/auth/login`；`current_student` 依赖 + `require_self` 越权门。
- 全部 `/students/{id}/*` + 会话/coop/diagnose 端点接入——**身份只认 token，不再信自报 id**，关审计 #3 IDOR。
- 前端 api.js 带 Authorization、401 回身份页；App.vue 身份页异步登录。
- **拍板**：方案 A（无密码 + 签名 token），防遍历不防冒充；将来加 PIN 即可。修了「登录后记录没了」（统一走 login 签发，token 与 id 绑定一致）。

### D3-4 Alembic 迁移体系 + updated_at + WAL（commit `4d1ba32`）
- Alembic 立起来：`env.py` 接 Base.metadata + 同源 DATABASE_URL；baseline 迁移（全表）。`init_db` 改 `alembic upgrade head`，含**老库纳管桥**（有表无 alembic_version → 先 stamp baseline 再 upgrade，不重复建表；异常兜底 create_all）。全新库/老库两条启动路径都验证。
- `tutor_sessions.updated_at` 列 + 迁移（nullable + 回填 created_at）；对话/运行/提交/coop 各路径 `touch()`；active-sessions 排序改用它。
- WAL + busy_timeout 从 D4-5 前移。

### D4-5 会话回合锁 + threadpool（commit `cffc81e`）
- `app/concurrency.py` per-session 内存锁（threading.Lock 分桶）；FastAPI yield-依赖 `turn_lock` 挂所有写会话端点——串行化同会话并发，堵 R2 丢写 + submit TOCTOU（审计 #12）。
- lifespan 抬 anyio threadpool 到 64。
- **判断**：async 全量重写 defer——同步端点跑在 threadpool 本就不阻塞事件循环，对 30 人目标够用。

## 里程碑 M2 · 过程化（代码链 + 时间线）— 已合并 main（merge `98e31b2`）

### 代码快照（commit `b5f77fd`）
- `CodeSnapshot` 表（append-only，链 execution_event）+ 迁移；`services/process.py` 的 `record_snapshot` + `compute_diff_stats`（`lines_changed` vs 上一版、`touched_mine_line` vs 原始雷行——盲改 vs 定向改信号）。run + submit 双写。
- 顺手修 alembic 坑（updated_at 的 model NOT NULL↔db nullable 落差生成 SQLite 不支持的 ALTER）：model 对齐 nullable，`alembic check` 无残留。

### ④失败回流喂「改动分析」（commit `f4e7598`）
- `process.diff_summary` 把 touched_mine_line 转成给导师的旁白；REPAIR_LAYER 据此分流：盲改→拉回②定位处；定向改错→回③根因对照，仍不给写法。旁白学生看不到、不照念。

### 消息时间线（commit 见 M2 merge）
- `SessionMessage` 表（append-only 真实动作时间线）+ `record_message`；run_turn/run_and_inject/submit/coop.message 埋 dual-write。
- **设计决策**：不彻底迁 `.history`——history 会删旧运行结果（避免污染 LLM 上下文），与回放「保留每一步」天然冲突，故两者各司其职（history=LLM 工作副本、session_messages=回放事实源）。移除 history 列留作后续清理。

## 里程碑 M3a · 过程回放页 — 已合并 main（merge `d24360f`，feat `0ddd36f`）
- `process.build_replay` 合并 code_snapshots + session_messages 按时间戳成时间线（code 步 join ExecutionEvent 带结果）；`GET /sessions/{id}/replay`（归属校验）。
- 前端 `Replay.vue` 叙事回放（代码逐版卡 + 结果徽章 + diff 大白话 + 对话气泡）+ 路由 `/replay/:sessionId`；入口挂成长轨迹每个 Episode「▶ 回看这道题的解题过程」。
- 端到端：`跑原始码→报错→改雷行→通过` 的完整故事可回放。

## 里程碑 M3b · 课程/单元 + 地图页 — 分支 `m3-course`
- **专项计划**：`docs/计划/2026-07-05-M3b课程地图计划.md`（先写计划、拍板决策 A1/B1/C1，再动手——首次走「先计划后动手」新流程）。
- **决策**：A1 单元定义走静态 `curriculum.yaml`、进度**纯从 knowledge_states 派生（零新表）**；B1 关卡全开放 + 推荐顺序；C1 只加地图入口、不动 1504 行的 Arena。
- `curriculum.yaml`：21 题编 4 单元（边界与差一/循环的陷阱/空值与共享/算术的坑），单元内按难度排关卡。
- `services/curriculum.py`：load_units + build_curriculum（单元→关卡 + 每关状态 + 复习到期标记 + 单元进度汇总，纯派生）。`GET /api/curriculum`（token 身份）。
- 前端 `Map.vue` + 路由 `/map` + 导航「课程地图」：单元卡（进度条）+ 关卡棋盘（序号/题名/难度/状态徽章 未接触·已解决✓·已内化★·复习🔁），点关卡→`/arena?start=pid`。
- 端到端：解一道题后地图该关「未接触→已解决」实时反映。测试 101→103。

## 里程碑 M4 · 教师端最小视图 — 分支 `m4-teacher`
- **专项计划**：`docs/计划/2026-07-05-M4教师端计划.md`（先计划、拍板 A1/B1/C1/D1 再动手）。
- **决策**：A1 复用 ACP_ADMIN_TOKEN；B1 看全体 + 可选 class_id 过滤（class_id 现全空，不改登录）；C1 隐藏 `/teacher` 页；D1 只做总览、钻取留后续。
- `services/teacher.py` build_class_overview：遍历 students（可 class_id 过滤），每人聚合 已解决/已内化/在学 + 最近活跃 + 正卡在哪题第几阶段，按活跃倒序，纯派生零新表。
- `GET /api/teacher/overview?token=&class_id=`：抽 `_check_admin` helper 复用 admin token 校验（admin_list_sessions 一并改用）。
- 前端隐藏页 `Teacher.vue` + 路由 `/teacher`（不挂学生导航）：输口令存本地 → 极简表格（学号/姓名/已解决/已内化/在学/最近活跃/正在进行）。
- 端到端：2 生做题 → 总览准确反映（u1 解 1 题停 ⑤验证、u2 卡 ①发现）；无/错 token 403。测试 103→104。

## 收尾 · #18 画像虚高修复（修法 A，分支 `m-profile-fix`）
- 用户拍板修法 A：维度分 = `acc / 全部子能力权重和`（未练的子能力按 0 计入分母），替代原「只对练过的求平均」。
- 效果：只练一项刷满不再把整维度顶到 100——只练独立调试 → Debug 能力 = 25；练 3 项各 80 → 60。分数直接反映"全面不全面"，最诚实。
- trained/total 覆盖度保留作语境；前端雷达图下加一句「分数按整个维度算——还没练的算 0」。
- 测试 104→105（新增「单练不顶满维度」断言）；已更新体检文档 #18 标记为已修。

## 附带产出
- 项目介绍文档 `docs/项目介绍-给学校.md` + 视频脚本 `docs/视频脚本-项目介绍.md`（面向全校铺开机会；视频已由作者制作完成）。
- 线上部署验证：Render `/api/health` 返回最新版本、`sandbox:subprocess`（fallback 正常）、题库在。

## 分支与部署
- 分支 `m1-hardening` / `m2-process` / `m3-replay` 均已 `--no-ff` 合并进 main 并 push；main 触发 Render 自动部署。
- 已知线上差异（诚实记录）：Render 无 docker → 沙箱走 subprocess（隔离弱、演示够用）；免费档 SQLite 每次部署清空。真给全校要上 VPS + docker + Postgres。

## 2026-07-06 收尾 · 缓冲项清理
- **开题干预短 TTL 缓存 ✅**：`timeline.recurring_for_student`（按学生 60s 内存缓存），`intervention_for` 改走它——开题不再每次全量重算 timeline（治体检 R4）。`build_timeline` 本身不缓存，成长轨迹页保持实时。测试 105→106。
- **计划标记收干净**：M2 父项 `[~]→[x]`；周末缓冲的测试补强标为已在 D1/D2-3 完成（沙箱逃逸 6 条 + 越权 3 条），pytest 迁移延后（收益低）；Arena 拆组件明确标为**独立里程碑延后**（大手术，不塞收尾）。
- 至此 v2 两周计划核心 + #18 + 开题缓存全部落地；剩余仅 Arena 拆组件（独立里程碑）与 pytest 迁移（可选）。

## 全部完成后的剩余（非核心，按需触发）
- Arena 拆组件（1504 行）+ 审计 #14-17 —— 独立里程碑。
- session_messages 彻底移除 history 列 —— dual-write 版工作正常，清理性质。
- #19 冷启动封顶 —— 调参，待拍板。
- 教师端钻取/学生分班/独立教师账号 —— 产品向，按需。
